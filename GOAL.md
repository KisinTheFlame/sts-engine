# 目标：完整复刻《杀戮尖塔》

你接手的是 `sts-engine`：一个用 TypeScript 逐位复刻《杀戮尖塔》的引擎。
仓库在 `~/Workspace/sts-engine`（**在 worktree 里跑**，见下方「环境」）。
已经做了四十七批，机器与纪律都成型了——**这是长期目标，不是一次性任务**，
按「一批」的粒度推进，每批做完停下来汇报，不要连着闷头做。

## 唯一的成功判据

**同种子逐位复现原版。** 不是「玩起来像」，是每一帧的血量、格挡、牌堆、
六条 RNG 流的计数器都逐位相同。

由此推出这个项目最重要的一条纪律，它凌驾于一切进度目标之上：

> **未验证的实现比没有实现更糟。**

没登记的内容会**显式抛错**，调用方立刻知道。登记了却错的内容会静默产出错误数值，
把「复现原版」变成谎话。所以 **「登记」不是完成，「有 trace 背书」才是**。

任何时候「为了赶进度先登记上、背书以后补」的念头出现，答案都是不行。
宁可交「五件其中一件卡住了，卡在哪写清楚」，也不要交「六件其中一件没背书」。

---

## 开工前必须读完这两份（它们是权威，本文件只是入口）

1. **`WORKFLOW.md`** —— 怎么做一轮。四十七批攒出来的全部硬经验都在里面，
   包括「怪物与编队特有的规矩」「爬升度轴」「目标策略轴」「遗物/药水这条轴」
   以及第 4 节**变异测试**（含「例数有两种假的」那一整节）。
2. **`TODOS.md`** —— 方向与账本。覆盖现状、三幕小结、盲区总清单（每条附**关门条件**）、
   「已知偏离参考项目之处」、「待裁定」。

⚠ **不要凭对游戏的印象写任何东西。** 参考项目是唯一的事实来源，
每一条关于它的断言在写进代码或任务书之前都必须有一次对应的 `grep -rn`（**全树，不是单个文件**）。

---

## 环境

- 引擎仓库在 worktree 里跑，**不要 cd 到主仓库**（否则提交会落到 master）。
- 参考项目：`~/Workspace/sts_lightspeed`，分支 **`sts-engine-harness`**（fork，带 harness）。
- 参考仓库的改动**必须提交**——数据的可复现性依赖它。
- 每批在当前分支提交，**不 push、不开 PR**，由人来推。

四条命令每批都要全绿：

```bash
pnpm typecheck && pnpm lint && pnpm test && pnpm format
```

---

## 北极星与参考项目的关系

**真实游戏是目标，`sts_lightspeed` 只是目前最好的预言机。**
它自己有 bug（已确认十几处，修了一部分）。发现疑似 bug 时**不要自己拍板**，
按三条判据走，**三条都成立才补**：

1. **在已登记的内容里真的产生分歧**（不是理论上的，要有例数）
2. **补丁有预言机**（补完能重新生成并重放验证）
3. **行为唯一确定**（是转写不是发明；文本上有几种等价拼法**不构成**障碍）

⚠ 写报告时**必须写清是哪一条先失败**，因为它决定将来怎么解锁：

- **③ 失败** = 没有真机 ground truth 就永远补不了。
- **① 失败** = 覆盖面扩大之后它会自己变成可补的。

⚠ 「隔壁那个是这样、这个不是」**本身不是证据**。要先问「判据是什么、两者在判据下同不同类」。
**参考自己跟自己矛盾**才是笔误的信号（兄弟表顺序不一致、同一函数里另两处写法都对、
声明了变量却从不写它）。

---

## 验证机制（不许绕过）

```bash
tools/regen-traces.sh --check      # 断言已提交数据能被逐字节复现
tools/regen-traces.sh --install …  # 生成 → 校验 → 装
```

**动 harness 必须两步走**：先只改管线、**不加任何 variant**，跑 `--check` 确认
**全部文件逐字节复现**（把输出贴进报告）；过了这一关才加 variant。
这一招四次开新轴（爬升度 / 目标策略 / 遗物药水 / 第三幕爬升度）全部用过，
每次都在真正动数据之前证明了「新维度对既有语料是空操作」。

**变异测试是这一步的核心，不是可选项。** 对拍全绿只说明代码与数据不冲突，
不说明数据看得见这段代码。每个「非直觉但照抄」的点都要改坏一次、看失败例数、还原。

0 例要**分三类**（结构性盲区 / 等价改写 / **探针无效**），
非 0 也要过判据（**假的非 0** 已出现五种形状）。全部形状见 WORKFLOW 第 4 节。
⚠ 廉价自查：**例数超过该宿主的分母，探针必然是错的**。

---

## 数据布局（碰之前先读 WORKFLOW，这里只列红线）

- **`traceIdx` 驱动遗物/药水轮换。** 任何改变某条 trace 之前有多少条 trace 的改动，
  都会平移其后全部数据。
- **`RELIC_ROTATION` / `POTION_ROTATION` 两张表的长度就是模数，已冻结。**
  要点名遗物/药水走 `DeckVariant` 的显式清单，**不要动那两张表**（实测追加一项 → 全库作废）。
- **第一幕那份 `encounters` 列表已冻结**，增删任一项都会平移。
- 往**乘积的编队列表**里加编队是免费的（不命中的 variant 在种子循环前 `continue`）；
  往 **variant 列表**里加不是。
- 新轴挂成**新乘积**。⚠ 更好的做法是让新乘积的 variant **钉死遗物与药水** ⇒
  它不读 `traceIdx` ⇒ **顺序无关**，谁也不冻结谁（第四十五批起用这条不变量取代了顺序约束）。

---

## 当前状态（截至第四十七批，会过时，以 TODOS 为准）

| 类别              | 已登记  | 全量              |
| ----------------- | ------- | ----------------- |
| 卡牌（铁甲+无色） | 108 + 1 | 116（可背书上限） |
| **怪物**          | **65**  | **65** ✅         |
| 编队              | 57      | 63                |
| 遗物              | 87      | 180               |
| 药水              | 28      | 42                |

208 个 trace 文件 / 42585 条 / 982MB / 全量测试 42874 例。
三幕都有 `{asc0, asc19}` 双档位；多怪编队另有 `@tgt1`；遗物/药水各有专门的乘积。

---

## 剩下的战线（这才是「完整复刻」的真实规模）

**做完的只是「铁甲的战斗」。** 剩下的按性质分三类：

1. **同一条线的延续**（机器已就绪，照批次往下铺）
   - 6 个事件编队；遗物 93 颗；药水 14 瓶
   - `asc7 + asc16` 一批（关掉「三档里的中间那一档」与阈值分辨，跨三幕，实现侧 0 行）
2. **别的角色**（要先补它们的机制）
   - 静默（中毒 / 飞刀）、缺陷（充能球 / 聚焦）、观者（姿态 / 灵蕴）
   - ⚠ 剩下的遗物药水**大部分卡在这里**，不是卡在工作量
3. **战斗之外**（harness 目前只驱动战斗，扩它本身是一次设计任务）
   - 地图 / 事件 / 商店 / 奖励 / Neow / 篝火
   - run 层接线缺口：`isRelicSupported` 导出了但**零调用者**；`RelicInstance.data`
     在战斗内一律从 0 起算，跨战斗延续没接
   - ⚠ 扩 harness 到 run 层，同样要走**两步验证**

---

## 怎么选下一批（价值排序，四十七批的经验）

1. **能关掉已记盲区的** > 单纯铺量。第四十批用六颗遗物一次关掉 12 条。
2. **一批关一整片的结构性缺口** > 逐件铺。开一条新轴（爬升度 / 目标策略）常常
   实现侧 0 行却点亮几十条死代码。
3. **先量再定**。十次经验：牌组/编队/遗物的选择都要**先量分母**再决定，不要推理。
   典型翻车：「新机制被走到了」不等于「够用」——一只怪有几道**互相独立的门**
   （出牌数 / 血量 / 回合数），就要各量一条分母。
4. **叠加会改变分母**。遗物可以免费叠**只对 `RelicContainer::add` 成立**，
   对覆盖密度不成立（硫磺让仗从 9.54 缩到 7.17；五颗加能量的让平均回合掉到 2.5）。

---

## 一批的形状与交付

**开发 → 生成预言机 → 验证 → 修复**，四步做完才算一批。

**细粒度提交**：管线 / 转写 / 数据 / **变异结果** / 文档，各自 commit。
⚠ **变异测试跑完立刻单独提交**——第二十一、二十四批都断在写文档那一步，
第二十四批那份变异表因此永久丢失。
⚠ **跑变异期间不提交、只开一个 runner**（第四十五批的 `git add -A` 把一条探针
一起提交了，靠下一次对拍才抓到）。

报告必须有：已登记清单（逐条 id + 参考 `文件:行号`）、跳过清单及理由、
新增/改动的共享原语与时点、**逐条变异例数**、0 例的三分类、不确定的点、四条命令结果。

---

## 最后：这个项目最有价值的产出不是内容表

是 **TODOS 里那份盲区清单与例数表**，以及 **WORKFLOW 里那些踩过的坑**。

四十七批里，**八个潜伏很久的错**全部是靠「换一个新维度的数据」暴露的，
没有一个是靠变异测试发现的——变异测试守的是「以后别改坏」，
新数据才是「现在就发现抄错了」。所以：

- 每批**完整重新生成**数据，不要只做增量验证。
- 盲区**必须记下来并附关门条件**，否则下一个人会以为它有背书。
- 关门条件要**可执行**（「牌组里放 ≥2 张混乱」），不是「等更多数据」。
- 记录被后续批次**证否**时，如实改掉并说明为什么错（这比多关一条盲区有用）。
